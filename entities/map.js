export function makeTiledMap(p, x, y) {
  return {
    tileWidth: 32,
    tileHeight: 32,
    x,
    y,

    async load() {

    },

    prepareTiles() {

    },

    getSpawnPoints() {

    },

    draw(camera, player) {
      
    }
  };
}